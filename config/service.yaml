llm:
  general:
    base_url: "http://<YOUR SERVER>/v1"
    bearer_token_env: "LLM_BEARER_TOKEN"
    bearer_token: "dev-key-123"
    model: "lfm2:latest"
    timeout_seconds: 240
    stream: true
  summarizer:
    base_url: "http://<YOUR SERVER>/v1"
    bearer_token_env: "LLM_BEARER_TOKEN"
    bearer_token: "dev-key-123"
    model: "lfm2:latest"
    timeout_seconds: 120
    stream: false
  verifier:
    base_url: "http://<YOUR SERVER>/v1"
    bearer_token_env: "LLM_BEARER_TOKEN"
    bearer_token: "dev-key-123"
    model: "lfm2:latest"
    timeout_seconds: 120
    stream: false

